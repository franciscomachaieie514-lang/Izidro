/* Izitrader realtime bridge — no React/CDN dependency.
 * OAuth session -> authenticated OTP WebSocket -> balance + ticks + contracts.
 */
(function(){
  'use strict';
  const SYMBOLS={R_10:'1HZ10V',R_25:'1HZ25V',R_50:'1HZ50V',R_75:'1HZ75V',R_100:'1HZ100V'};
  const SYMBOL_NAMES={R_10:'Volatility 10',R_25:'Volatility 25',R_50:'Volatility 50',R_75:'Volatility 75',R_100:'Volatility 100'};
  const CONTRACTS={EVEN:'DIGITEVEN',ODD:'DIGITODD',OVER:'DIGITOVER',UNDER:'DIGITUNDER',RISE:'CALL',FALL:'PUT',DIFFER:'DIGITDIFF',MATCH:'DIGITMATCH'};
  let ws=null,stopped=false,reconnectTimer=null,reconnectDelay=1000;
  let accountType=localStorage.getItem('izitrader_account_type')||'real';
  let uiSymbol='R_100',symbol='1HZ100V',botName='ProParity',stake=Math.max(.35,Number(localStorage.getItem('izitrader_stake')||.35));
  let ticks=[],active=false,buying=false,proposal=null,contractId=null,pnl=0,contractSubscription=null;
  const $=s=>document.querySelector(s);const setText=(s,v)=>{const e=$(s);if(e)e.textContent=v};const dispatch=(name,detail)=>window.dispatchEvent(new CustomEvent(name,{detail}));
  function accountId(a){return a&&(a.account_id||a.accountId||a.id||a.loginid)}
  function isReal(a){const t=String(a&&(a.account_type||a.type)||'').toLowerCase();return t==='real'||(a&&a.is_virtual===false)||/^CR/i.test(String(accountId(a)||''))}
  function isDemo(a){const t=String(a&&(a.account_type||a.type)||'').toLowerCase();return t==='demo'||(a&&a.is_virtual===true)||/^VR/i.test(String(accountId(a)||''))}
  function showError(msg){setStatus(String(msg||'Erro Deriv'),false);dispatch('izitrader:deriv-error',{message:String(msg||'Erro Deriv')})}
  function setStatus(text,connected){const dot=$('.status .dot');if(dot)dot.style.background=connected?'#35d492':'#f5a623';setText('#statusText',text)}
  function currentBot(){const e=$('#selectedBotName');if(e&&e.textContent.trim())botName=e.textContent.trim()}
  function currentStake(){const e=$('#stakeInput');if(e){const n=Number(String(e.value).replace(',','.'));if(Number.isFinite(n)&&n>=.35)stake=Number(n.toFixed(2));return}const old=$('.stake-value');if(old){const n=Number(old.textContent.replace(/[^0-9.,]/g,'').replace(',','.'));if(Number.isFinite(n)&&n>0)stake=Math.max(.35,n)}}
  function currentSymbol(){const e=$('.symbol-dropdown .dropdown-btn');if(e){const selected=e.querySelector('#selectedSymbol');const v=selected?.dataset?.symbolCode||selected?.textContent.trim();if(SYMBOLS[v]){uiSymbol=v;symbol=SYMBOLS[v]}}}
  function digit(quote,pip){const n=Number(quote);if(!Number.isFinite(n))return null;const p=Number.isInteger(Number(pip))?Number(pip):2;return Number(n.toFixed(Math.max(0,Math.min(10,p))).slice(-1))}
  function strategy(){const n=String(botName).toUpperCase();if(/RISE|FALL|SUBIR|DESCER/.test(n))return'RISE_FALL';if(/DIFER|DIFF/.test(n))return'DIFF';if(/MATCH/.test(n))return'MATCH';if(/OVER|UNDER|ACIMA|BAIXO/.test(n))return'OVER_UNDER';return'EVEN_ODD'}
  function getSignal(){if(ticks.length<5)return null;const v=ticks.slice(-5),d=v.map(x=>digit(x.quote,x.pip_size)).filter(x=>x!==null);if(d.length<5)return null;const s=strategy();if(s==='EVEN_ODD'){const e=d.filter(x=>x%2===0).length/5*100,o=100-e;return e>=65?['EVEN',e]:o>=65?['ODD',o]:null}if(s==='OVER_UNDER'){const o=d.filter(x=>x>5).length/5*100,u=d.filter(x=>x<4).length/5*100;return o>=65?['OVER',o]:u>=65?['UNDER',u]:null}if(s==='DIFF'){const x=d.filter(x=>x!==0).length/5*100;return x>=65?['DIFFER',x]:null}if(s==='MATCH'){const x=d.filter(x=>x===0).length/5*100;return x>=65?['MATCH',x]:null}let up=0,down=0;for(let i=1;i<v.length;i++){if(Number(v[i].quote)>Number(v[i-1].quote))up++;else if(Number(v[i].quote)<Number(v[i-1].quote))down++}const total=up+down||1;return up/total*100>=65?['RISE',up/total*100]:down/total*100>=65?['FALL',down/total*100]:null}
  function send(obj){if(!ws||ws.readyState!==WebSocket.OPEN)return false;ws.send(JSON.stringify(obj));return true}
  async function getJson(url){const r=await fetch(url,{credentials:'same-origin',cache:'no-store'}),j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||('HTTP '+r.status));return j}
  function renderHistoryOpen(c){const h=$('#historyScroll');if(!h)return;const item=document.createElement('div');item.className='history-item';item.dataset.contractId=String(c.contract_id||'');const amount=Number(c.buy_price??c.price??stake);item.innerHTML=`<div class="bot">${botName} • ${accountType==='real'?'REAL':'DEMO'}</div><div class="digit-row"><span class="digit">Aposta $${Number.isFinite(amount)?amount.toFixed(2):stake.toFixed(2)}</span><span class="result">ABERTO</span></div><div class="time">${new Date().toLocaleTimeString()}</div>`;h.prepend(item);while(h.children.length>20)h.lastElementChild.remove()}
  function renderHistoryClosed(c,profit){const h=$('#historyScroll');if(!h)return;let item=[...h.children].find(x=>x.dataset.contractId===String(c.contract_id||''));if(!item){renderHistoryOpen(c);item=[...h.children].find(x=>x.dataset.contractId===String(c.contract_id||''))}if(!item)return;const won=profit>=0,r=item.querySelector('.result'),d=item.querySelector('.digit');if(d)d.textContent=`${won?'Ganho':'Perda'} ${won?'+':'-'}$${Math.abs(profit).toFixed(2)}`;if(r){r.textContent=won?'WIN':'LOSS';r.className='result '+(won?'win':'loss')}}
  function setupStakeUI(){const row=$('.stake-row');if(!row)return;let input=$('#stakeInput');if(!input){const old=row.querySelector('.stake-value');if(old)old.remove();input=document.createElement('input');input.id='stakeInput';input.type='number';input.min='0.35';input.step='0.01';input.inputMode='decimal';input.value=stake.toFixed(2);input.style.cssText='width:120px;background:var(--field);border:1px solid var(--border);border-radius:10px;padding:9px 10px;color:var(--text);font-size:22px;font-weight:800;text-align:right;outline:none';row.appendChild(input);input.addEventListener('change',()=>{let n=Number(String(input.value).replace(',','.'));if(!Number.isFinite(n)||n<.35)n=.35;n=Number(n.toFixed(2));stake=n;input.value=n.toFixed(2);localStorage.setItem('izitrader_stake',String(n));dispatch('izitrader:stake',{stake:n})})}}
  function setupSymbols(){document.querySelectorAll('.symbol-item').forEach(item=>{const code=item.dataset.symbol;if(!code)return;const name=item.querySelector('.symbol-name'),desc=item.querySelector('.symbol-desc');if(name)name.textContent=SYMBOL_NAMES[code]||code;if(desc)desc.textContent=code});const selected=$('#selectedSymbol');if(selected){selected.textContent=SYMBOL_NAMES[uiSymbol]||uiSymbol;selected.dataset.symbolCode=uiSymbol}}
  async function connect(){if(stopped||ws)return;currentBot();currentStake();currentSymbol();setStatus(`A ligar à conta ${accountType==='real'?'Real':'Demo'}…`,false);try{const aj=await getJson('/api/deriv/accounts'),accounts=Array.isArray(aj.accounts)?aj.accounts:[],account=accountType==='real'?accounts.find(isReal):accounts.find(isDemo);if(!account)throw new Error(`Conta ${accountType==='real'?'Real':'Demo'} não encontrada pela API Deriv`);const id=accountId(account);if(!id)throw new Error('A API Deriv não devolveu account_id');const wj=await getJson('/api/deriv/ws-url?account_id='+encodeURIComponent(id)),url=wj&&((wj.data&&wj.data.url)||wj.wsUrl);if(!url)throw new Error('Deriv não devolveu URL WebSocket autenticada');if(stopped)return;ws=new WebSocket(url);ws.onopen=()=>{reconnectDelay=1000;setStatus(`${accountType==='real'?'Conta Real':'Conta Demo'} ligada — ${botName}`,true);send({balance:1,subscribe:1,req_id:1});send({ticks:symbol,subscribe:1,req_id:2});send({profit_table:1,limit:500,offset:0,req_id:3});dispatch('izitrader:ws-open',{accountType,accountId:id,symbol,uiSymbol})};ws.onmessage=event=>handleMessage(event.data);ws.onerror=()=>showError(`Erro no WebSocket ${accountType==='real'?'Real':'Demo'}`);ws.onclose=()=>{ws=null;setStatus(`Reconectando à conta ${accountType==='real'?'Real':'Demo'}…`,false);if(!stopped){clearTimeout(reconnectTimer);reconnectTimer=setTimeout(connect,reconnectDelay);reconnectDelay=Math.min(reconnectDelay*2,10000)}}}catch(e){showError(e.message||'Falha ao ligar à Deriv');if(!stopped){clearTimeout(reconnectTimer);reconnectTimer=setTimeout(connect,reconnectDelay);reconnectDelay=Math.min(reconnectDelay*2,10000)}}}
  function syncProfitTable(table){const rows=Array.isArray(table)?table:Array.isArray(table&&table.transactions)?table.transactions:[];if(!rows.length)return;let total=0;rows.forEach(row=>{const p=Number(row?.profit_loss);if(Number.isFinite(p))total+=p;else{const buy=Number(row?.buy_price),sell=Number(row?.sell_price);if(Number.isFinite(buy)&&Number.isFinite(sell))total+=sell-buy}});if(Number.isFinite(total)){pnl=total;setText('#pnl',(pnl>=0?'+':'')+'$'+pnl.toFixed(2));applyPnlColor();dispatch('izitrader:pnl',{profit_loss:pnl,accountType,source:'profit_table'})}}
  function handleMessage(raw){let m;try{m=JSON.parse(raw)}catch{return}if(m.error){showError(m.error.message||'Erro Deriv');return}if(m.msg_type==='balance'&&m.balance){const value=Number(m.balance.balance),currency=m.balance.currency||'USD';if(Number.isFinite(value)){setText('#balance',(currency==='USD'?'$':currency+' ')+value.toFixed(2));dispatch('izitrader:balance',{balance:value,currency,loginid:m.balance.loginid})}return}if(m.msg_type==='profit_table'&&m.profit_table){syncProfitTable(m.profit_table);return}if(m.msg_type==='tick'&&m.tick){ticks.push(m.tick);if(ticks.length>1000)ticks.shift();dispatch('izitrader:tick',{quote:m.tick.quote,quoteRaw:String(m.tick.quote),epoch:m.tick.epoch,symbol:m.tick.symbol,pip_size:m.tick.pip_size});const sig=getSignal();dispatch('izitrader:live-digit',{digit:digit(m.tick.quote,m.tick.pip_size),quote:m.tick.quote,epoch:m.tick.epoch});if(active&&sig)requestProposal(sig);return}if(m.msg_type==='proposal'&&m.proposal){proposal=m.proposal;if(active)buyProposal();return}if(m.msg_type==='buy'&&m.buy){buying=false;proposal=null;contractId=Number(m.buy.contract_id)||null;renderHistoryOpen({...m.buy,buy_price:m.buy.buy_price??m.buy.price});dispatch('izitrader:buy',{...m.buy,accountType});if(contractId)send({proposal_open_contract:1,contract_id:contractId,subscribe:1,req_id:Date.now()%100000});return}if(m.msg_type==='proposal_open_contract'&&m.proposal_open_contract){const c=m.proposal_open_contract;if(m.subscription&&m.subscription.id)contractSubscription=m.subscription.id;if(c.is_sold||['sold','won','lost','expired'].includes(String(c.status||'').toLowerCase())){const profit=Number(c.profit_loss??c.profit);if(Number.isFinite(profit)){pnl+=profit;setText('#pnl',(pnl>=0?'+':'')+'$'+pnl.toFixed(2));applyPnlColor();renderHistoryClosed(c,profit)}dispatch('izitrader:contract-closed',{...c,result:String(c.status||'').toUpperCase(),profit_loss:profit,accountType});contractId=null;proposal=null;buying=false;if(contractSubscription)send({forget:contractSubscription});contractSubscription=null}}}
  function requestProposal(sig){if(!active||buying||proposal||contractId)return;currentStake();const type=sig[0],ct=CONTRACTS[type];if(!ct)return;const p={proposal:1,amount:stake,basis:'stake',contract_type:ct,currency:'USD',duration:1,duration_unit:'t',underlying_symbol:symbol,req_id:Date.now()%100000};if(type==='OVER')p.barrier='5';if(type==='UNDER')p.barrier='4';if(type==='DIFFER'||type==='MATCH')p.barrier='0';send(p)}
  function buyProposal(){if(!active||buying||!proposal)return;const id=proposal.id||proposal.proposal_id,price=Number(proposal.ask_price||proposal.display_value);if(!id||!Number.isFinite(price)||price<=0){showError('Proposal inválida para BUY');proposal=null;return}buying=true;send({buy:String(id),price,req_id:Date.now()%100000})}
  function bindUI(){setupStakeUI();setupSymbols();const sel=$('#iziAccountSelect');if(sel&&!sel.dataset.bound){sel.dataset.bound='1';sel.value=accountType;sel.addEventListener('change',()=>{active=false;accountType=sel.value;localStorage.setItem('izitrader_account_type',accountType);stop();connect()})}const btn=$('.operate-btn');if(btn&&!btn.dataset.iziBound){btn.dataset.iziBound='1';btn.addEventListener('click',()=>{if(!ws)return;if(accountType==='real'&&!active&&!window.confirm('ATENÇÃO: isto permite COMPRAS REAIS na sua conta Deriv. Deseja continuar?'))return;active=!active;btn.textContent=active?(accountType==='real'?'PARAR ROBÔ REAL':'PARAR ROBÔ DEMO'):(accountType==='real'?'INICIAR ROBÔ REAL':'INICIAR ROBÔ DEMO')})}if(btn){btn.disabled=!ws;btn.textContent=active?(accountType==='real'?'PARAR ROBÔ REAL':'PARAR ROBÔ DEMO'):(accountType==='real'?'INICIAR ROBÔ REAL':'INICIAR ROBÔ DEMO')}}
  function applyPnlColor(){const p=$('#pnl');if(!p)return;const n=Number(String(p.textContent).replace(/[^0-9.+-]/g,''));p.style.color=n<0?'#e24b4a':n>0?'#f5c04a':'#fff'}
  function stop(){clearTimeout(reconnectTimer);reconnectTimer=null;if(ws){try{ws.close()}catch{}ws=null}ticks=[];proposal=null;contractId=null;buying=false;setStatus('A ligar…',false)}
  function boot(){const h=$('#historyScroll');if(h)h.innerHTML='';setText('#balance','A carregar...');setText('#pnl','$0.00');setText('#lastDigit','–');bindUI();applyPnlColor();setInterval(()=>{bindUI();currentBot();currentStake();currentSymbol();applyPnlColor();const b=$('.operate-btn');if(b)b.disabled=!ws},500);connect()}
  window.IziDerivWS={connect,stop,setAccountType:t=>{accountType=t;localStorage.setItem('izitrader_account_type',t);stop();connect()},send};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
